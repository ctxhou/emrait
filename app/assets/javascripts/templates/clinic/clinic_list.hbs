<h3>你的所在位置: {{lat}}, {{lng}}</h3>
<h4 style="color: red">注意：依據科別檢索時，因部份診所並未在衛福部登記科別資訊，故無法呈現。</h4>
{{#if items}}
    <table class="table">
        <thead>
            <tr>
                <th>#</th>
                <th>診所名稱</th>
                <th>距離(m)</th>
                <th></th>
            </tr>
        </thead>
        {{#each items}}
            <tr>
                <th>{{@index}}</th>
                <td>{{id}}</td>
                <td>{{distance}}</td>
                <td><a class="glyphicon glyphicon-pushpin js-clinic-location" data-name="{{id}}" aria-hidden="true"></a></td>
            </tr>
        {{/each}}
    </table>
{{/if}}
