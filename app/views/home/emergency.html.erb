<div class="pull-right"><a href="/mission">出勤中救護車</a></div>
<h2>鄰近救護車列表</h2>
<div class="h4">有<%= @injure %>位病患</div>
<table class="table">
    <thead>
        <tr>
            <th>距離(km)</th>
            <th>機構名稱</th>
            <th>電話</th>
            <th>救護車數</th>
            <th>建議</th>
            <th></th>
        </tr>
    </thead>
    <% @abmulance_hash.each do |distance, data| %>
    <tr>
        <td><%= distance %></td>
        <td><%= data["name"] %></td>
        <td><%= data["phone"] %></td>
        <td><%= data["number"] %></td>
        <td>
            <% if @suggest_ambulance[distance] %>
                <input type="number" value="<%= @suggest_ambulance[distance] %>" min="0" max="<%= data["number"] %>" class="js-assign" data-id="<%= data["id"] %>">
            <% else %>
                <input type="number" value="0" min="0" max="<%= data["number"] %>" class="js-assign" data-id="<%= data["id"] %>">
            <% end %>
        </td>
        <td>
            <button class="glyphicon glyphicon-info-sign js-ambulance" data-id="<%= data["id"] %>" data-toggle="modal" data-target="#myModal"></button>
        </td>
    </tr>
    <% end %>
</table>
<button class="btn btn-success pull-right" id="assign-submit">一鍵指派</button>

<% if @shelter_hash %>
    <h2>鄰近災民收容所列表</h2>
    <div class="h4">有<%= @victims %>位災民</div>
    <table class="table">
        <thead>
            <tr>
                <th>距離(km)</th>
                <th>收容所名稱</th>
                <th>縣市</th>
                <th>地址</th>
            </tr>
        </thead>
        <% @shelter_hash.each do |distance, ary| %>
            <tr>
                <td><%= distance %></td>
                <td><%= ary["災民收容所名稱"] %></td>
                <td><%= ary["所在縣市"] %></td>
                <td><%= ary["收容所地址"] %></td>
            </tr>
        <% end %>
    </table>
<% end %>
<%= render "units/bootstrap_modal" %>
<input type="hidden" value="<%= @lat %>" id="end_lat">
<input type="hidden" value="<%= @lng %>" id="end_lng">
<% content_for :javascript do %>
<script src="//maps.googleapis.com/maps/api/js?v=3.exp&libraries=geometry"></script>
<script async="async" data-turbolinks-track="true" src="/assets/dist/emergency.js"></script>
<% end %>